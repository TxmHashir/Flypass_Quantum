import java.sql.Connection;

public class DBConnection {
    public static Connection getConnection() {
        // Return null or throw an exception that you catch
        // Since we modified UserDAO, this shouldn't even be called now.
        return null; 
    }
}